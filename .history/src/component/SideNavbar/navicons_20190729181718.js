import {
  homeIcon, communityIcon, fileIcon, powerIcon, twitterIcon, settingIcon,
} from '../../images/index';

const sideNav = [
  {
    title: 'Home',
    img: '',
  },
];

export default sideNav;
